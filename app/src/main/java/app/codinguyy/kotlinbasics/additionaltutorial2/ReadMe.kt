package app.codinguyy.kotlinbasics.additionaltutorial2

/**
 * Enum classes, constructors, properties, functions
 * sealed classes
 * nested and inner classes
 * an inner class can access the outer class members
 * companion objects
 * keyword object
 * extensions
 * use labels with brake, continue, return
 * infix
 * inline
 */